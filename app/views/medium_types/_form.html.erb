<%= form_with(model: medium_type) do |form| %>
  <% if medium_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medium_type.errors.count, "error") %> prohibited this medium_type from being saved:</h2>

      <ul>
        <% medium_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
